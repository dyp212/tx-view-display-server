<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.txrd.system.modular.user.mapper.SysUserMapper">


    <select id="selectUserRoles" resultType="com.txrd.common.vo.RoleVo">
        select r.*
        from sys_role r
        left join sys_user_role ur on r.id=ur.role_id
        where ur.user_id=#{userId}
    </select>

    <select id="selectPermissions" resultType="com.txrd.common.vo.PermissionVo">
        select p.*
        from sys_permission p
        left join sys_role_permission rp on p.id=rp.permission_id
        left join sys_user_role ur on ur.role_id=rp.role_id
        where ur.user_id=#{userId}
    </select>
</mapper>
